"use strict";
/* import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import * as dotenv from 'dotenv'; // Importar dotenv

dotenv.config();  // Cargar las variables de entorno desde el archivo .env
console.log('JWT_SECRET:', process.env.JWT_SECRET);  // Verifica que la clave secreta esté disponible


@Module({
  imports: [
    JwtModule.register({
      secret: process.env.JWT_SECRET,  // Usar la variable de entorno JWT_SECRET
      signOptions: { expiresIn: '1h' },
    }),
  ],
})
export class jwtModule {}
 */ 
